{
  "name": "nocms-validation",
  "version": "1.0.0",
  "description": "Validation rules for form",
  "main": "lib/index.js",
  "scripts": {
    "test": "tape test/*.js | tap-spec",
    "build": "babel ./src --out-dir ./lib --source-maps --presets es2015,stage-2",
    "coverage": "tap --cov --coverage-report=lcov test/*.js",
    "prepublish": "babel ./src --out-dir ./lib --source-maps --presets es2015,stage-2 && browserify ./lib/index.js -o ./build/nocms-validation.js --standalone NoCMSValidation && uglifyjs ./build/nocms-validation.js --compress --mangle --output ./build/nocms-validation.min.js --source-map ./build/nocms-validation.min.js.map"
  },
  "dependencies": {
    "moment": "^2.13.0",
    "norwegian-validation": "git+ssh://git@bitbucket.org/nocms/norwegian-validation.git"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.8.0",
    "babel-eslint": "^6.0.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-2": "^6.5.0",
    "browserify": "^13.0.1",
    "eslint": "^3.12.2",
    "eslint-config-webteam": "git+https://github.com/miles-no/eslint-config-webteam.git",
    "eslint-plugin-import": "^2.2.0",
    "tap": "^5.8.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0",
    "uglify-js": "^2.6.4"
  },
  "author": "",
  "license": "ISC"
}
